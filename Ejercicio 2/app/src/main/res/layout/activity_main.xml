<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="16dp">

    <!-- Campo de entrada para la ciudad -->
    <EditText
        android:id="@+id/editTextCity"
        android:textColor="@color/black"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Ingrese una ciudad"
        android:textColorHint="#c2c1c1"
        android:padding="8dp"
        android:background="@android:drawable/editbox_background"/>

    <!-- Botón para buscar el clima -->
    <Button
        android:id="@+id/buttonSearch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Buscar Clima"
        android:layout_gravity="center"
        android:backgroundTint="#f54600"
        android:layout_marginTop="16dp"/>

    <!-- Texto para mostrar el nombre de la ciudad -->
    <TextView
        android:id="@+id/textViewCity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Ciudad: ---"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_marginTop="24dp"
        android:layout_gravity="center"/>

    <!-- Texto para mostrar la temperatura -->
    <TextView
        android:id="@+id/textViewTemp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Temperatura: ---°C"
        android:textSize="18sp"
        android:textColor="@android:color/holo_blue_dark"
        android:textStyle="bold"
        android:layout_marginTop="16dp"
        android:layout_gravity="center"/>

    <!-- Texto para mostrar la humedad -->
    <TextView
        android:id="@+id/textViewHumidity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Humedad: ---%"
        android:textSize="16sp"
        android:layout_marginTop="8dp"
        android:layout_gravity="center"/>

    <!-- Texto para mostrar la descripción del clima -->
    <TextView
        android:id="@+id/textViewDescription"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Condiciones: ---"
        android:textSize="16sp"
        android:textStyle="italic"
        android:layout_marginTop="8dp"
        android:layout_gravity="center"/>

</LinearLayout>
